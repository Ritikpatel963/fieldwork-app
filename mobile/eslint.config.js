// ESLint v9+ uses the "flat config" format by default.
// This project already depends on `eslint-config-universe`, so we reuse its
// React Native preset.
module.exports = require('eslint-config-universe/flat/native');

